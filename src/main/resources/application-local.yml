spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rinha-de-backend
    username: admin
    password: admin
  jpa:
    hibernate.ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect